apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bg-products-svc.fullname" . }}-test-connection"
  labels:
    {{- include "bg-products-svc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bg-products-svc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
